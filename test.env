# Test Environment Configuration
# This file is used for testing only

# Server
PORT=5002
GIN_MODE=test

# Database (use in-memory SQLite for tests)
DB_HOST=localhost
DB_PORT=5432
DB_USER=hyberica
DB_PASSWORD=hyberica
DB_NAME=go_base_api_test

# Redis (test instance)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1

# JWT (test keys - never use in production)
JWT_SECRET_KEY=test-jwt-secret-key-for-testing-only
JWT_ACCESS_DURATION=1h
JWT_REFRESH_DURATION=24h

# Logging
LOG_LEVEL=error
LOG_FORMAT=json

# Security
REQUEST_SIZE_LIMIT=1048576
RATE_LIMIT_REQUESTS=1000

# Test specific
GO_ENV=test
